import PostItem from '~/components/PostItem';

const Home = (props) => {
    return (
        <>
            <PostItem />
            <PostItem />
            <PostItem />
        </>
    );
};

export default Home;
